export const HKDF_SALTED_ENCRYPTED_PREFIX = "%=";

export async function decryptWithEphemeralSalt(input) {
  return input;
}

export async function decrypt(input) {
  return input;
}

export async function encrypt(input) {
  return input;
}
