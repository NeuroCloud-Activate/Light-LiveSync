{
  "name": "light-livesync",
  "version": "0.1.44",
  "private": true,
  "description": "Light-LiveSync keeps Obsidian vaults synced through a self-hosted CouchDB backend with low-contention encrypted sync.",
  "type": "module",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs dev",
    "package": "npm run build && node scripts/package-release.mjs && node scripts/verify-release.mjs",
    "verify:release": "node scripts/verify-release.mjs",
    "test": "npm run build && node tests/run-all.mjs",
    "check": "npm run typecheck && npm test && npm run package",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "octagonal-wheels": "^0.1.45",
    "xxhash-wasm-102": "npm:xxhash-wasm@^1.0.2"
  },
  "devDependencies": {
    "@types/node": "^24.10.13",
    "esbuild": "0.25.0",
    "obsidian": "^1.12.3",
    "typescript": "5.9.3"
  }
}
